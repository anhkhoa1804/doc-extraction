# 042 control discovery status

No control population is frozen yet. The source-only role scout is
annotation-only and outcome-independent. It cannot infer the current
production `Region.label` from DocLayNet categories because the source
annotation vocabulary has no `document_index` category.

Natural `document_index` controls therefore require an unchanged baseline
layout replay on the already-frozen 279-page development cohort. The replay
must observe raw production roles and invocation provenance; it must not use
TableTransformer treatment, GT geometry, or role normalization.

The acquisition integrity gate passed before this conclusion. The current
branch is `CONTROL_SUPPLY_INADEQUATE_BUT_INFORMATIVE`, not a claim that the
production role is absent from the new corpus.
