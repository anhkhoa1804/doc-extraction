# Experiment 038 — independent real-corpus activation

This is the real-corpus successor to the correctly blocked synthetic 037
attempt.  The corpus source is DocLayNet validation data, acquired from the
official DocLayNet archive by bounded HTTP ranges.  Raw annotations, images,
and baseline runs live under the ignored `results/` directory.

The experiment is not considered activated until the unchanged production
baseline discovers a non-zero, defensible development strict-D2 population.
